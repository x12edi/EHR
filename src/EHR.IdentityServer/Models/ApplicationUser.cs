using Microsoft.AspNetCore.Identity;

namespace EHR.IdentityServer.Models
{
    public class ApplicationUser : IdentityUser
    {
        // Add identity-specific fields if needed (rarely overlaps with domain user)
        // e.g., for extra login metadata
    }
}
